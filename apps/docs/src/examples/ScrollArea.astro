---
import { ScrollArea } from '@santi020k/lumen-astro'
---

<ScrollArea>
  <p>Lumen keeps markup semantic and portable across frameworks.</p>
  <p>Styles live in one shared stylesheet driven by design tokens.</p>
  <p>Progressive enhancement adds behavior without a framework runtime.</p>
  <p>Astro, React, and Web Components share the same class contract.</p>
</ScrollArea>
