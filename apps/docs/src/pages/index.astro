---

/* cspell:ignore aaronmgz Aarón commitprompt coolstead */

import {
  Alert,
  AnimatedNumber,
  Badge,
  Button,
  ButtonGroup,
  ButtonLink,
  Card,
  Checkbox,
  Eyebrow,
  Field,
  Icon,
  Image,
  Input,
  Label,
  Marker,
  RevealGroup,
  ScrollReveal,
  Stat,
  Switch,
  Tabs
} from '@santi020k/lumen-astro'

import HomeWorkbench from '../components/HomeWorkbench.astro'
import { frameworkSetups } from '../data/docs'
import type { DocsPlatformId } from '../data/platforms'
import { productPlatformGuides } from '../data/platforms'
import { documentedComponentCount, primaryPageMetadata } from '../data/site-metadata'
import BaseLayout from '../layouts/BaseLayout.astro'

const componentCount = documentedComponentCount
const aiSkillInstallCommand = 'npx skills add santi020k/lumen --skill lumen-ui'

const installerIcons = {
  astro: 'asterisk',
  elements: 'blocks',
  react: 'atom'
} as const

const heroInstallers = frameworkSetups.map(setup => ({
  command: setup.installCommands.find(install => install.label === 'pnpm')?.command ?? '',
  icon: installerIcons[setup.id],
  id: setup.id,
  label: setup.label
}))

const platformPresentation = {
  android: {
    icon: 'bot',
    preview: '/native-previews/android-compose.png',
    runtime: 'Jetpack Compose · Material 3'
  },
  apple: {
    icon: 'apple',
    preview: '/native-previews/apple-native.png',
    runtime: 'SwiftUI · five Apple platforms'
  },
  foundations: {
    icon: 'layers-3',
    preview: undefined,
    runtime: 'One canonical token source'
  },
  'react-native': {
    icon: 'smartphone',
    preview: '/native-previews/react-native-android.png',
    runtime: 'React Native · Expo ready'
  },
  web: {
    icon: 'panels-top-left',
    preview: undefined,
    runtime: 'Astro · React · Web Components'
  }
} satisfies Record<DocsPlatformId, {
  icon: string
  preview: string | undefined
  runtime: string
}>

const platformShowcase = productPlatformGuides.map(guide => ({
  ...guide,
  ...platformPresentation[guide.id]
}))

const principles = [
  {
    description: 'One semantic token and component contract keeps web and native products recognizably aligned.',
    icon: 'layers-3',
    metric: '4 platforms',
    title: 'Change platforms, not your design language.'
  },
  {
    description: 'Keyboard paths, focus management, labels, and meaningful markup are part of the primitive.',
    icon: 'shield-check',
    metric: 'A11y first',
    title: 'The accessible path is the default path.'
  },
  {
    description: 'Interactive Astro primitives use a tiny progressive-enhancement layer, not an app runtime.',
    icon: 'zap',
    metric: '~4 KB',
    title: 'Keep the interaction. Lose the weight.'
  }
] as const

const categories = [
  'Actions',
  'Forms',
  'Navigation',
  'Overlays',
  'Data display',
  'Feedback',
  'Layout',
  'Brand'
] as const

interface UseCase {
  category: string
  description: string
  featured?: boolean
  href: string
  desktop: {
    height: number
    src: string
    width: number
  }
  mobile: {
    height: number
    src: string
    width: number
  }
  name: string
  stack: string
}

const useCases: readonly UseCase[] = [
  {
    category: 'Developer quality',
    description: 'A deterministic Astro health-check toolkit spanning a CLI, ESLint plugin, editor extension, GitHub Action, docs, and agent skills.',
    desktop: { height: 895, src: '/use-cases/astro-doctor-desktop-live.jpg', width: 1432 },
    featured: true,
    href: 'https://doctor.santi020k.com',
    mobile: { height: 827, src: '/use-cases/astro-doctor-mobile-live.jpg', width: 382 },
    name: 'Astro Doctor',
    stack: 'Astro · TypeScript'
  },
  {
    category: 'iOS photo studio',
    description: 'An iPhone-first visual content studio with a cinematic product story for private, on-device photo selection, enhancement, and composition.',
    desktop: { height: 900, src: '/use-cases/postlens-desktop-live.jpg', width: 1440 },
    href: 'https://postlens.santi020k.com',
    mobile: { height: 844, src: '/use-cases/postlens-mobile-live.jpg', width: 390 },
    name: 'PostLens',
    stack: 'Astro · SwiftUI'
  },
  {
    category: 'macOS utility',
    description: 'A safety-minded native Mac utility with a calm product surface for thermal monitoring and gradual, bounded fan control.',
    desktop: { height: 900, src: '/use-cases/coolstead-desktop-live.jpg', width: 1440 },
    href: 'https://coolstead.santi020k.com',
    mobile: { height: 844, src: '/use-cases/coolstead-mobile-live.jpg', width: 390 },
    name: 'Coolstead',
    stack: 'Astro · SwiftUI'
  }
]

---

<BaseLayout
  description={primaryPageMetadata.home.description}
  title={primaryPageMetadata.home.title}
>
  <main class="home">
    <section class="home-hero">
      <div class="home-hero__wash" aria-hidden="true"></div>
      <RevealGroup class="home-hero__copy" stagger={70}>
        <Eyebrow class="home-hero__eyebrow">
          <Marker variant="success">Open source</Marker>
          <span>{componentCount} crafted primitives</span>
        </Eyebrow>
        <h1>
          Build interfaces<br />
          people <em>want</em> to use.
        </h1>
        <p class="home-hero__lede">
          A cross-platform UI system for Web, React Native, Apple platforms, and Android.
          Accessible by default, beautifully tokenized, and native to every environment.
        </p>
        <div class="home-hero__actions">
          <ButtonLink class="home-button-lg" href="/docs" showArrow variant="primary">
            Start building
          </ButtonLink>
          <ButtonLink class="home-button-lg" href="/docs/components" variant="secondary">
            Explore components
          </ButtonLink>
        </div>
        <Tabs class="home-installer" glass="subtle">
          <span class="home-installer__label">Web quick install</span>
          <div role="tablist" aria-label="Choose a web framework to install">
            {heroInstallers.map((installer, index) => (
              <button
                aria-controls={`hero-install-panel-${installer.id}`}
                aria-selected={index === 0 ? 'true' : 'false'}
                id={`hero-install-tab-${installer.id}`}
                role="tab"
              >
                <Icon name={installer.icon} size="sm" />
                {installer.label}
              </button>
            ))}
          </div>
          {heroInstallers.map((installer, index) => (
            <section
              aria-labelledby={`hero-install-tab-${installer.id}`}
              hidden={index !== 0}
              id={`hero-install-panel-${installer.id}`}
              role="tabpanel"
            >
              <Button
                class="home-install"
                data-copy-command={installer.command}
                variant="ghost"
              >
                <Icon name="terminal" size="sm" />
                <code>{installer.command}</code>
                <span class="home-install__copy" data-copy-feedback>Copy</span>
              </Button>
            </section>
          ))}
        </Tabs>
      </RevealGroup>

      <ScrollReveal animation="scale" class="home-hero__product" delay={140}>
        <div class="home-hero__product-label" aria-hidden="true">
          <span>Actual primitives</span>
          <i></i>
          <span>Live canvas</span>
        </div>
        <HomeWorkbench />
      </ScrollReveal>
    </section>

    <section class="home-proof" aria-label="Lumen at a glance">
      <div class="home-container home-proof__inner">
        <p>One system. Every surface.</p>
        <div class="home-proof__stats">
          <Stat label="components">
            <span class="ui-stat-value"><AnimatedNumber value={componentCount} /></span>
          </Stat>
          <Stat label="product platforms">
            <span class="ui-stat-value"><AnimatedNumber value={4} /></span>
          </Stat>
          <Stat label="runtime lock-in">
            <span class="ui-stat-value"><AnimatedNumber value={0} /></span>
          </Stat>
        </div>
        <div class="home-proof__categories" aria-label="Component categories">
          {categories.map(category => <Badge variant="outline">{category}</Badge>)}
        </div>
      </div>
    </section>

    <section class="home-section home-container home-platforms" aria-labelledby="platforms-title">
      <ScrollReveal animation="slide-up">
        <div class="home-section__head home-section__head--split">
          <div>
            <Eyebrow>Built for every surface</Eyebrow>
            <h2 id="platforms-title">One design language.<br />Web and three native platforms.</h2>
          </div>
          <div class="home-section__aside">
            <p>
              Share semantic color, type, spacing, motion, and component intent—then let each
              platform render and behave the way its users expect.
            </p>
            <Marker variant="success">Native adapters are supported for Lumen 2</Marker>
            <ButtonLink href="/docs/foundations" showArrow variant="inline">
              Explore shared foundations
            </ButtonLink>
          </div>
        </div>
      </ScrollReveal>

      <RevealGroup class="home-platforms__grid" stagger={70}>
        {platformShowcase.map((platform, index) => (
          <Card
            as="article"
            class:list={[
              'home-platform',
              { 'home-platform--web': platform.id === 'web' }
            ]}
          >
            <div class="home-platform__visual">
              {platform.preview ?
(
                <Image
                  alt={`${platform.label} Lumen component gallery`}
                  height={platform.id === 'apple' ? 1440 : 1920}
                  src={platform.preview}
                  width={platform.id === 'apple' ? 1640 : 1080}
                />
              ) :
(
                <div class="home-platform__web-canvas" aria-hidden="true">
                  <span><Icon decorative name="asterisk" size="sm" />Astro</span>
                  <span><Icon decorative name="atom" size="sm" />React</span>
                  <span><Icon decorative name="blocks" size="sm" />Elements</span>
                  <div>
                    <i></i><i></i><i></i>
                  </div>
                </div>
              )}
              <span class="home-platform__number" aria-hidden="true">0{index + 1}</span>
            </div>
            <div class="home-platform__body">
              <div class="home-platform__meta">
                <span><Icon decorative name={platform.icon} size="sm" />{platform.runtime}</span>
                <div>
                  <Marker variant="success">{platform.status}</Marker>
                </div>
              </div>
              <h3>{platform.label}</h3>
              <p>{platform.summary}</p>
              <code>{platform.packageName}</code>
              <ButtonLink href={platform.href} showArrow variant="inline">
                Build for {platform.shortLabel}
              </ButtonLink>
            </div>
          </Card>
        ))}
      </RevealGroup>
    </section>

    <section class="home-section home-container" aria-labelledby="components-title">
      <ScrollReveal animation="slide-up">
        <div class="home-section__head home-section__head--split">
          <div>
            <Eyebrow>Designed as a system</Eyebrow>
            <h2 id="components-title">The pieces already know<br />how to work together.</h2>
          </div>
          <div class="home-section__aside">
            <p>
              Production-ready controls with consistent states, spacing, motion,
              and behavior—composed into real interface patterns.
            </p>
            <ButtonLink href="/docs/components" showArrow variant="inline">
              Browse all {componentCount} components
            </ButtonLink>
          </div>
        </div>
      </ScrollReveal>

      <RevealGroup class="home-specimens" stagger={60}>
        <Card class="home-specimen home-specimen--actions">
          <div class="home-specimen__label">
            <span>Actions</span>
            <Badge variant="secondary">7 variants</Badge>
          </div>
          <div class="home-specimen__center">
            <ButtonGroup>
              <Button>
                <Icon name="plus" size="sm" />
                Create
              </Button>
              <Button variant="secondary">Duplicate</Button>
              <Button aria-label="More actions" size="icon" variant="outline">
                <Icon name="ellipsis" size="sm" />
              </Button>
            </ButtonGroup>
          </div>
          <p>Buttons, links, groups, toggles, and toolbars share one visual rhythm.</p>
        </Card>

        <Card class="home-specimen home-specimen--form">
          <div class="home-specimen__label">
            <span>Forms</span>
            <Marker variant="success">Valid</Marker>
          </div>
          <div class="home-specimen__form">
            <Field>
              <Label for="home-email">Work email</Label>
              <Input id="home-email" placeholder="you@company.com" type="email" />
            </Field>
            <label>
              <Checkbox checked />
              <span>Send me product updates</span>
            </label>
            <Button>Join the preview</Button>
          </div>
        </Card>

        <Card class="home-specimen home-specimen--feedback">
          <div class="home-specimen__label">
            <span>Feedback</span>
            <Badge variant="secondary">Live states</Badge>
          </div>
          <div class="home-specimen__alerts">
            <Alert variant="success">
              <Icon name="circle-check" size="sm" />
              <div>
                <strong>Changes published</strong>
                <p>Your new theme is live everywhere.</p>
              </div>
            </Alert>
            <Alert variant="warning">
              <Icon name="triangle-alert" size="sm" />
              <div>
                <strong>Review contrast</strong>
                <p>Accent text needs a darker surface.</p>
              </div>
            </Alert>
          </div>
        </Card>

        <Card class="home-specimen home-specimen--tokens">
          <div class="home-specimen__label">
            <span>Semantic tokens</span>
            <Switch aria-label="Enable brand theme" checked />
          </div>
          <div class="home-specimen__swatches" aria-label="Token colors">
            <span style="--swatch: hsl(var(--brand));">brand</span>
            <span style="--swatch: hsl(var(--accent));">accent</span>
            <span style="--swatch: hsl(var(--success));">success</span>
            <span style="--swatch: hsl(var(--warning));">warning</span>
            <span style="--swatch: hsl(var(--danger));">danger</span>
          </div>
          <p>Theme the meaning once. Every primitive follows.</p>
        </Card>
      </RevealGroup>
    </section>

    <section class="home-section home-container home-use-cases" aria-labelledby="use-cases-title">
      <ScrollReveal animation="slide-up">
        <div class="home-section__head home-section__head--split">
          <div>
            <Eyebrow>Lumen in production</Eyebrow>
            <h2 id="use-cases-title">One system.<br />Very different products.</h2>
          </div>
          <div class="home-section__aside">
            <p>
              One shared foundation, three unmistakable identities. Explore real Lumen products
              across desktop and mobile—not component demos, but finished experiences.
            </p>
          </div>
        </div>
      </ScrollReveal>

      <RevealGroup class="home-use-cases__grid" stagger={45}>
        {useCases.map(useCase => (
          <Card
            as="article"
            class:list={[
              'home-use-case',
              { 'home-use-case--featured': useCase.featured }
            ]}
          >
            <div class="home-use-case__stage">
              <div class="home-use-case__browser">
                <div class="home-use-case__browser-bar" aria-hidden="true">
                  <span><i></i><i></i><i></i></span>
                  <code>{new URL(useCase.href).hostname.replace('www.', '')}</code>
                </div>
                <Image
                  alt={`${useCase.name} website on desktop`}
                  height={useCase.desktop.height}
                  src={useCase.desktop.src}
                  width={useCase.desktop.width}
                />
              </div>
              <div class="home-use-case__phone">
                <span aria-hidden="true"></span>
                <Image
                  alt={`${useCase.name} website on mobile`}
                  height={useCase.mobile.height}
                  src={useCase.mobile.src}
                  width={useCase.mobile.width}
                />
              </div>
              <Badge class="home-use-case__viewport-badge" variant="secondary">
                <Icon decorative name="panels-top-left" size="sm" />
                Desktop + mobile
              </Badge>
            </div>
            <div class="home-use-case__body">
              <div class="home-use-case__meta">
                <Badge variant="outline">{useCase.category}</Badge>
                <span>{useCase.stack}</span>
              </div>
              <h3>{useCase.name}</h3>
              <p>{useCase.description}</p>
              <ButtonLink
                ariaLabel={`Open ${useCase.name} in a new tab`}
                href={useCase.href}
                newTab
                showArrow
                variant="inline"
              >
                Experience the project
              </ButtonLink>
            </div>
          </Card>
        ))}
      </RevealGroup>
    </section>

    <section class="home-principles">
      <div class="home-container">
        <ScrollReveal animation="slide-up">
          <div class="home-section__head">
            <Eyebrow>Quietly rigorous</Eyebrow>
            <h2>Craft you can see.<br />Details you don’t have to chase.</h2>
          </div>
        </ScrollReveal>
        <RevealGroup class="home-principles__grid" stagger={90}>
          {principles.map((principle, index) => (
            <article class="home-principle">
              <span class="home-principle__number">0{index + 1}</span>
              <Icon name={principle.icon} />
              <Badge variant="outline">{principle.metric}</Badge>
              <h3>{principle.title}</h3>
              <p>{principle.description}</p>
            </article>
          ))}
        </RevealGroup>
      </div>
    </section>

    <section class="home-section home-container home-ai" id="ai-native" aria-labelledby="ai-title">
      <ScrollReveal animation="slide-up">
        <div class="home-ai__copy">
          <Eyebrow>AI-native UI</Eyebrow>
          <h2 id="ai-title">Give your coding agent<br />the Lumen design system.</h2>
          <p>
            Install one portable skill for Codex, Claude Code, Cursor, Windsurf, and other Agent
            Skills clients. It teaches your AI to choose real Lumen primitives, respect the current
            framework, use semantic tokens, and verify accessible interactions.
          </p>
          <div class="home-ai__actions">
            <ButtonLink href="/docs/ai-skill" showArrow variant="primary">
              Explore the AI skill
            </ButtonLink>
            <ButtonLink href="/docs/mcp" variant="inline">
              Add live MCP context
            </ButtonLink>
          </div>
        </div>
      </ScrollReveal>
      <ScrollReveal animation="scale" delay={100}>
        <Card class="home-ai__terminal" glass="subtle">
          <div class="home-ai__terminal-head">
            <div class="home-ai__terminal-dots" aria-hidden="true">
              <span></span><span></span><span></span>
            </div>
            <Badge variant="outline">Agent Skills</Badge>
          </div>
          <div class="home-ai__terminal-line">
            <span>$</span>
            <code>{aiSkillInstallCommand}</code>
          </div>
          <div class="home-ai__agent-flow">
            <span><Icon decorative name="scan-search" size="sm" /> Detect framework</span>
            <span><Icon decorative name="blocks" size="sm" /> Select primitives</span>
            <span><Icon decorative name="palette" size="sm" /> Apply tokens</span>
            <span><Icon decorative name="shield-check" size="sm" /> Verify behavior</span>
          </div>
          <Button
            class="home-ai__copy-command"
            data-copy-command={aiSkillInstallCommand}
            variant="ghost"
          >
            <Icon decorative name="copy" size="sm" />
            <span data-copy-feedback>Copy install command</span>
          </Button>
        </Card>
      </ScrollReveal>
    </section>

  </main>
</BaseLayout>

<script>
  const initHomeCopy = () => {
    for (const button of document.querySelectorAll<HTMLButtonElement>('[data-copy-command]')) {
      if (button.dataset.copyInit === 'true') continue

      button.dataset.copyInit = 'true'

      button.addEventListener('click', async () => {
        const command = button.dataset.copyCommand

        if (!command) return

        try {
          await navigator.clipboard.writeText(command)

          const feedback = button.querySelector('[data-copy-feedback]')

          if (!feedback) return

          feedback.textContent = 'Copied'

          setTimeout(() => {
            feedback.textContent = 'Copy'
          }, 1800)
        } catch {
          // Clipboard access can be unavailable in non-secure preview contexts.
        }
      })
    }
  }

  initHomeCopy()

  document.addEventListener('astro:page-load', initHomeCopy)
</script>

<style>
  .home {
    overflow: hidden;
    padding-bottom: clamp(5rem, 10vw, 9rem);
  }

  .home-container {
    width: min(100% - 2.5rem, 74rem);
    margin-inline: auto;
  }

  .home-hero {
    position: relative;
    padding: clamp(5rem, 10vw, 8.5rem) 0 0;
  }

  .home-hero__wash {
    position: absolute;
    top: -12rem;
    left: 50%;
    z-index: -1;
    width: min(100rem, 130vw);
    height: 52rem;
    transform: translateX(-50%);
    background:
      radial-gradient(circle at 30% 35%, hsl(var(--brand) / .17), transparent 31%),
      radial-gradient(circle at 70% 30%, hsl(var(--accent) / .12), transparent 28%);
    filter: blur(18px);
    pointer-events: none;
  }

  .home-hero__copy {
    display: flex;
    width: min(100% - 2.5rem, 61rem);
    margin-inline: auto;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .home-hero__eyebrow {
    display: inline-flex;
    align-items: center;
    gap: .65rem;
    margin-bottom: 1.5rem;
  }

  .home-hero h1 {
    max-width: 13ch;
    margin: 0;
    color: hsl(var(--ink));
    font-family: var(--app-font-display), "Montserrat", sans-serif;
    font-size: clamp(3.25rem, 7.4vw, 6.65rem);
    font-weight: 800;
    letter-spacing: -.045em;
    line-height: .96;
  }

  .home-hero h1 em {
    position: relative;
    display: inline-block;
    background: linear-gradient(110deg, hsl(var(--brand)), hsl(var(--accent)));
    background-clip: text;
    color: transparent;
    font-style: normal;
  }

  .home-hero h1 em::after {
    position: absolute;
    right: -.04em;
    bottom: -.08em;
    left: -.04em;
    height: .08em;
    border-radius: 999px;
    background: linear-gradient(90deg, hsl(var(--brand)), hsl(var(--accent)));
    content: "";
    transform: rotate(-1.5deg);
  }

  .home-hero__lede {
    max-width: 42rem;
    margin: 1.8rem 0 0;
    color: hsl(var(--ink-soft));
    font-size: clamp(1rem, 1.7vw, 1.22rem);
    line-height: 1.7;
  }

  .home-hero__actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: .75rem;
    margin-top: 1.8rem;
  }

  .home-button-lg {
    min-height: 3.25rem;
    padding-inline: 1.55rem;
  }

  .home-installer {
    display: grid;
    width: min(100%, 45rem);
    margin-top: 1.1rem;
    grid-template-columns: auto auto minmax(0, 1fr);
    align-items: center;
    gap: .35rem;
    border: 1px solid hsl(var(--line) / .8);
    border-radius: 999px;
    background:
      linear-gradient(180deg, hsl(var(--canvas) / .38), transparent),
      hsl(var(--surface) / .66);
    box-shadow:
      0 12px 38px hsl(var(--ink) / .055),
      inset 0 1px 0 hsl(var(--canvas) / .9);
    padding: .3rem;
    backdrop-filter: blur(18px);
  }

  .home-installer__label {
    padding-inline: .8rem .45rem;
    color: hsl(var(--ink-muted));
    font-size: .62rem;
    font-weight: 800;
    letter-spacing: .09em;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .home-installer [role="tablist"] {
    width: auto;
    justify-content: start;
    gap: .1rem;
    border: 0;
    border-radius: 999px;
    background: hsl(var(--surface-muted) / .7);
    padding: .18rem;
  }

  .home-installer [role="tab"] {
    display: inline-flex;
    min-width: 0;
    align-items: center;
    justify-content: center;
    gap: .32rem;
    border-radius: 999px;
    padding: .34rem .55rem;
    font-size: .72rem;
  }

  .home-installer [role="tab"][aria-selected="true"] {
    background: hsl(var(--surface) / .92);
    color: hsl(var(--brand));
    box-shadow: 0 2px 10px hsl(var(--ink) / .08);
  }

  .home-installer [role="tabpanel"] {
    min-width: 0;
    border-left: 1px solid hsl(var(--line) / .8);
  }

  .home-install {
    width: 100%;
    min-width: 0;
    justify-content: flex-start;
    gap: .65rem;
    border: 0;
    border-radius: 999px;
    background: transparent;
    padding-inline: .8rem;
  }

  .home-install code {
    min-width: 0;
    overflow: hidden;
    flex: 1;
    color: hsl(var(--ink-soft));
    font-size: .78rem;
    text-align: left;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .home-install__copy {
    color: hsl(var(--brand));
    font-size: .64rem;
    font-weight: 800;
    letter-spacing: .04em;
    text-transform: uppercase;
  }

  .home-hero__product {
    position: relative;
    width: min(100% - 2.5rem, 82rem);
    margin: clamp(3.5rem, 7vw, 6rem) auto 0;
  }

  .home-hero__product::before {
    position: absolute;
    inset: -2rem 5% 30%;
    z-index: -1;
    border-radius: 50%;
    background: hsl(var(--brand) / .14);
    content: "";
    filter: blur(55px);
  }

  .home-hero__product-label {
    display: flex;
    align-items: center;
    gap: .65rem;
    margin: 0 0 .8rem .5rem;
    color: hsl(var(--ink-muted));
    font-size: .66rem;
    font-weight: 800;
    letter-spacing: .08em;
    text-transform: uppercase;
  }

  .home-hero__product-label i {
    width: 2.5rem;
    height: 1px;
    background: hsl(var(--line));
  }

  .home-proof {
    border-top: 1px solid hsl(var(--line) / .7);
    border-bottom: 1px solid hsl(var(--line) / .7);
    background: hsl(var(--surface-muted) / .35);
    margin-top: 5rem;
    padding: 2rem 0;
  }

  .home-proof__inner {
    display: grid;
    grid-template-columns: auto auto minmax(0, 1fr);
    align-items: center;
    gap: 2.25rem;
  }

  .home-proof__inner > p {
    margin: 0;
    color: hsl(var(--ink));
    font-family: var(--app-font-display), sans-serif;
    font-weight: 800;
  }

  .home-proof__stats {
    display: flex;
    gap: 1.5rem;
    border-right: 1px solid hsl(var(--line));
    border-left: 1px solid hsl(var(--line));
    padding-inline: 2rem;
  }

  .home-proof__stats :global(.ui-stat-value) {
    color: hsl(var(--ink));
    font-family: var(--app-font-display), sans-serif;
    font-size: 1.4rem;
    font-weight: 800;
  }

  .home-proof__stats :global(.ui-stat-label) {
    color: hsl(var(--ink-muted));
    font-size: .62rem;
  }

  .home-proof__categories {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: .4rem;
  }

  .home-section {
    padding-top: clamp(6rem, 11vw, 10rem);
  }

  .home-section__head {
    display: grid;
    gap: .75rem;
  }

  .home-section__head--split {
    grid-template-columns: minmax(0, 1.35fr) minmax(17rem, .65fr);
    align-items: end;
    gap: 3rem;
  }

  .home-section__head h2 {
    margin: 0;
    color: hsl(var(--ink));
    font-family: var(--app-font-display), "Montserrat", sans-serif;
    font-size: clamp(2.2rem, 5vw, 4rem);
    font-weight: 800;
    letter-spacing: -.03em;
    line-height: 1.08;
  }

  .home-section__aside {
    display: grid;
    justify-items: start;
    gap: 1rem;
  }

  .home-section__aside p {
    margin: 0;
    color: hsl(var(--ink-soft));
    font-size: .95rem;
    line-height: 1.7;
  }

  .home-specimens {
    display: grid;
    grid-template-columns: repeat(12, minmax(0, 1fr));
    gap: .9rem;
    margin-top: 3rem;
  }

  .home-specimen {
    display: flex;
    min-height: 18rem;
    flex-direction: column;
    gap: 1rem;
    overflow: hidden;
    border-color: hsl(var(--line) / .86);
    box-shadow: none;
    padding: 1.15rem;
  }

  .home-specimen--actions { grid-column: span 7; }
  .home-specimen--form { grid-column: span 5; }
  .home-specimen--feedback { grid-column: span 7; }
  .home-specimen--tokens { grid-column: span 5; }

  .home-specimen__label {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    color: hsl(var(--ink));
    font-size: .74rem;
    font-weight: 800;
  }

  .home-specimen__center {
    display: grid;
    min-height: 10rem;
    place-items: center;
    border-radius: .9rem;
    background:
      linear-gradient(hsl(var(--line) / .35) 1px, transparent 1px),
      linear-gradient(90deg, hsl(var(--line) / .35) 1px, transparent 1px),
      hsl(var(--surface-muted) / .45);
    background-size: 24px 24px;
  }

  .home-specimen > p {
    margin: auto 0 0;
    color: hsl(var(--ink-muted));
    font-size: .78rem;
    line-height: 1.6;
  }

  .home-specimen__form {
    display: grid;
    gap: .8rem;
    margin: auto 0;
    border: 1px solid hsl(var(--line) / .75);
    border-radius: 1rem;
    background: hsl(var(--surface-muted) / .45);
    padding: 1rem;
  }

  .home-specimen__form > label {
    display: flex;
    align-items: center;
    gap: .55rem;
    color: hsl(var(--ink-soft));
    font-size: .72rem;
  }

  .home-specimen__form :global(.ui-input) {
    width: 100%;
  }

  .home-specimen__alerts {
    display: grid;
    gap: .75rem;
    margin: auto 0;
  }

  .home-specimen__alerts :global(.ui-alert) {
    display: flex;
    align-items: flex-start;
    gap: .75rem;
  }

  .home-specimen__alerts strong {
    font-size: .82rem;
  }

  .home-specimen__alerts p {
    margin: .2rem 0 0;
    font-size: .72rem;
  }

  .home-specimen__swatches {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: .55rem;
    margin: auto 0;
  }

  .home-specimen__swatches span {
    display: grid;
    gap: .5rem;
    color: hsl(var(--ink-muted));
    font-size: .58rem;
    text-align: center;
  }

  .home-specimen__swatches span::before {
    height: 5.5rem;
    border: 1px solid hsl(var(--line) / .5);
    border-radius: .7rem;
    background: var(--swatch);
    content: "";
  }

  .home-use-cases__grid {
    display: grid;
    grid-template-columns: repeat(12, minmax(0, 1fr));
    gap: 1.25rem;
    margin-top: 3rem;
  }

  .home-use-case {
    display: flex;
    min-width: 0;
    grid-column: span 6;
    flex-direction: column;
    overflow: hidden;
    border-color: hsl(var(--line) / .78);
    background: hsl(var(--surface) / .88);
    box-shadow: 0 24px 64px -46px hsl(var(--ink) / .68);
    padding: 0;
  }

  .home-use-case--featured {
    display: grid;
    grid-column: 1 / -1;
    grid-template-columns: minmax(0, 1.65fr) minmax(18rem, .75fr);
  }

  .home-use-case__stage {
    position: relative;
    min-height: 24rem;
    overflow: hidden;
    border-bottom: 1px solid hsl(var(--line) / .72);
    background:
      radial-gradient(circle at 78% 28%, hsl(var(--brand) / .2), transparent 34%),
      linear-gradient(145deg, hsl(var(--surface-muted)), hsl(var(--surface-strong)));
    padding: clamp(1.4rem, 3vw, 2.5rem) clamp(2.1rem, 5vw, 4.5rem) clamp(2rem, 4vw, 3.5rem)
      clamp(1.1rem, 2.5vw, 2rem);
  }

  .home-use-case--featured .home-use-case__stage {
    min-height: 31rem;
    border-right: 1px solid hsl(var(--line) / .72);
    border-bottom: 0;
  }

  .home-use-case__stage::before {
    position: absolute;
    inset: 0;
    background-image: linear-gradient(hsl(var(--line) / .24) 1px, transparent 1px),
      linear-gradient(90deg, hsl(var(--line) / .24) 1px, transparent 1px);
    background-size: 2.25rem 2.25rem;
    mask-image: linear-gradient(to bottom right, black, transparent 70%);
    content: "";
    pointer-events: none;
  }

  .home-use-case__browser {
    position: relative;
    z-index: 1;
    overflow: hidden;
    border: 1px solid hsl(var(--line));
    border-radius: .85rem;
    background: hsl(var(--canvas));
    box-shadow: 0 2rem 4rem -2rem hsl(var(--ink) / .6);
  }

  .home-use-case__browser-bar {
    display: grid;
    height: 2rem;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: .65rem;
    border-bottom: 1px solid hsl(var(--line) / .75);
    background: hsl(var(--surface));
    padding-inline: .7rem;
  }

  .home-use-case__browser-bar > span {
    display: flex;
    gap: .28rem;
  }

  .home-use-case__browser-bar i {
    width: .42rem;
    height: .42rem;
    border-radius: 50%;
    background: hsl(var(--line));
  }

  .home-use-case__browser-bar code {
    overflow: hidden;
    color: hsl(var(--ink-muted));
    font-size: .56rem;
    text-align: center;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .home-use-case__browser :global(.ui-image) {
    display: block;
    width: 100%;
    aspect-ratio: 16 / 9;
    object-fit: cover;
    object-position: top center;
  }

  .home-use-case__phone {
    position: absolute;
    z-index: 2;
    right: clamp(.65rem, 2.5vw, 1.6rem);
    bottom: clamp(.7rem, 2vw, 1.35rem);
    width: clamp(5.6rem, 21%, 8.2rem);
    overflow: hidden;
    border: 3px solid hsl(var(--ink));
    border-radius: 1.3rem;
    background: hsl(var(--ink));
    box-shadow: 0 1.2rem 2.8rem hsl(var(--ink) / .42);
  }

  .home-use-case__phone > span {
    position: absolute;
    z-index: 1;
    top: .35rem;
    left: 50%;
    width: 32%;
    height: .28rem;
    transform: translateX(-50%);
    border-radius: 999px;
    background: hsl(var(--ink));
  }

  .home-use-case__phone :global(.ui-image) {
    display: block;
    width: 100%;
    aspect-ratio: 390 / 844;
    object-fit: cover;
    object-position: top center;
  }

  .home-use-case__viewport-badge {
    position: absolute;
    z-index: 3;
    bottom: 1rem;
    left: 1rem;
    display: inline-flex;
    align-items: center;
    gap: .35rem;
    box-shadow: 0 .8rem 2rem hsl(var(--ink) / .18);
  }

  .home-use-case__body {
    display: flex;
    min-height: 16rem;
    flex-direction: column;
    align-items: flex-start;
    padding: clamp(1.25rem, 2.5vw, 1.8rem);
  }

  .home-use-case--featured .home-use-case__body {
    min-height: 0;
    padding: clamp(1.6rem, 3.5vw, 2.8rem);
  }

  .home-use-case__meta {
    display: flex;
    width: 100%;
    align-items: center;
    justify-content: space-between;
    gap: .75rem;
  }

  .home-use-case__meta > span {
    overflow: hidden;
    color: hsl(var(--ink-muted));
    font-size: .62rem;
    font-weight: 750;
    letter-spacing: .04em;
    text-overflow: ellipsis;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .home-use-case h3 {
    margin: 1.3rem 0 0;
    color: hsl(var(--ink));
    font-family: var(--app-font-display), sans-serif;
    font-size: clamp(1.3rem, 2.1vw, 1.7rem);
    font-weight: 750;
    letter-spacing: -.02em;
    line-height: 1.15;
  }

  .home-use-case__body > p {
    margin: .7rem 0 1.25rem;
    color: hsl(var(--ink-soft));
    font-size: .82rem;
    line-height: 1.65;
  }

  .home-use-case__body > :global(.ui-button-link) {
    margin-top: auto;
  }

  @media (prefers-reduced-motion: no-preference) {
    .home-use-case__browser,
    .home-use-case__phone {
      transition: transform 420ms cubic-bezier(.22, 1, .36, 1);
    }

    .home-use-case:hover .home-use-case__browser {
      transform: translateY(-.2rem);
    }

    .home-use-case:hover .home-use-case__phone {
      transform: translateY(-.45rem) rotate(1.5deg);
    }
  }

  .home-principles {
    margin-top: clamp(6rem, 11vw, 10rem);
    border-top: 1px solid hsl(var(--line) / .7);
    border-bottom: 1px solid hsl(var(--line) / .7);
    background:
      radial-gradient(circle at 85% 15%, hsl(var(--brand) / .12), transparent 28%),
      hsl(var(--surface-muted) / .42);
    padding: clamp(5rem, 9vw, 8rem) 0;
  }

  .home-principles__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    margin-top: 3.5rem;
    border-top: 1px solid hsl(var(--line));
    border-bottom: 1px solid hsl(var(--line));
  }

  .home-principle {
    position: relative;
    display: grid;
    align-content: start;
    gap: 1rem;
    min-height: 21rem;
    padding: 2rem;
  }

  .home-principle + .home-principle {
    border-left: 1px solid hsl(var(--line));
  }

  .home-principle__number {
    color: hsl(var(--ink-muted));
    font-family: var(--santi020k-font-mono, monospace);
    font-size: .7rem;
  }

  .home-principle > :global(.ui-icon) {
    width: 2rem;
    height: 2rem;
    margin-top: 1.5rem;
    color: hsl(var(--brand));
  }

  .home-principle > :global(.ui-badge) {
    justify-self: start;
  }

  .home-principle h3 {
    max-width: 19rem;
    margin: auto 0 0;
    color: hsl(var(--ink));
    font-family: var(--app-font-display), sans-serif;
    font-size: 1.35rem;
    font-weight: 600;
    line-height: 1.2;
  }

  .home-principle p {
    margin: 0;
    color: hsl(var(--ink-soft));
    font-size: .85rem;
    line-height: 1.65;
  }

  .home-ai {
    display: grid;
    grid-template-columns: minmax(0, .9fr) minmax(24rem, 1.1fr);
    gap: clamp(2rem, 6vw, 5rem);
    align-items: center;
  }

  .home-ai__copy {
    display: grid;
    justify-items: start;
    gap: 1rem;
  }

  .home-ai__copy h2 {
    margin: 0;
    color: hsl(var(--ink));
    font-family: var(--app-font-display), "Montserrat", sans-serif;
    font-size: clamp(2.2rem, 5vw, 3.8rem);
    font-weight: 800;
    letter-spacing: -.03em;
    line-height: 1.08;
  }

  .home-ai__copy > p {
    max-width: 36rem;
    margin: .4rem 0 0;
    color: hsl(var(--ink-soft));
    font-size: .95rem;
    line-height: 1.75;
  }

  .home-ai__actions {
    display: flex;
    flex-wrap: wrap;
    gap: .65rem;
    align-items: center;
    margin-top: .5rem;
  }

  .home-ai__terminal {
    display: grid;
    gap: 1rem;
    overflow: hidden;
    border-color: hsl(var(--brand) / .22);
    background:
      radial-gradient(circle at 90% 0%, hsl(var(--brand) / .16), transparent 34%),
      hsl(var(--surface) / .82);
    box-shadow: 0 28px 72px -52px hsl(var(--ink) / .55);
    padding: 1rem;
  }

  .home-ai__terminal-head {
    display: flex;
    align-items: center;
    gap: .35rem;
  }

  .home-ai__terminal-dots {
    display: flex;
    gap: .35rem;
  }

  .home-ai__terminal-dots span {
    width: .55rem;
    height: .55rem;
    border-radius: 999px;
    background: hsl(var(--line));
  }

  .home-ai__terminal-head :global(.ui-badge) {
    margin-left: auto;
  }

  .home-ai__terminal-line {
    display: flex;
    gap: .65rem;
    overflow-x: auto;
    border: 1px solid hsl(var(--line) / .8);
    border-radius: .8rem;
    background: hsl(var(--canvas) / .78);
    padding: .9rem;
    color: hsl(var(--brand));
    font: 700 .76rem/1.5 var(--santi020k-font-mono, monospace);
    white-space: nowrap;
  }

  .home-ai__terminal-line code {
    color: hsl(var(--ink));
  }

  .home-ai__agent-flow {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: .55rem;
  }

  .home-ai__agent-flow span {
    display: flex;
    align-items: center;
    gap: .55rem;
    border-radius: .7rem;
    background: hsl(var(--surface-muted) / .72);
    padding: .7rem;
    color: hsl(var(--ink-soft));
    font-size: .72rem;
    font-weight: 700;
  }

  .home-ai__agent-flow :global(.ui-icon) {
    color: hsl(var(--brand));
  }

  .home-ai__copy-command:global(.ui-button) {
    justify-self: start;
    color: hsl(var(--ink-soft));
  }

  .home-platforms {
    position: relative;
  }

  .home-platforms::before {
    position: absolute;
    top: clamp(8rem, 15vw, 13rem);
    right: -12%;
    z-index: -1;
    width: 28rem;
    height: 28rem;
    border-radius: 50%;
    background: hsl(var(--brand) / .1);
    filter: blur(90px);
    content: "";
    pointer-events: none;
  }

  .home-platforms__grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
    margin-top: clamp(2.5rem, 5vw, 4rem);
  }

  .home-platform {
    display: grid;
    min-width: 0;
    grid-template-rows: 18rem 1fr;
    overflow: hidden;
    border-color: hsl(var(--line) / .78);
    background: hsl(var(--surface) / .9);
    box-shadow: 0 26px 72px -58px hsl(var(--ink) / .72);
    padding: 0;
  }

  .home-platform__visual {
    position: relative;
    display: grid;
    min-width: 0;
    place-items: end center;
    overflow: hidden;
    border-bottom: 1px solid hsl(var(--line) / .72);
    background:
      linear-gradient(hsl(var(--line) / .23) 1px, transparent 1px),
      linear-gradient(90deg, hsl(var(--line) / .23) 1px, transparent 1px),
      radial-gradient(circle at 50% 118%, hsl(var(--brand) / .22), transparent 48%),
      hsl(var(--surface-muted) / .72);
    background-size: 2rem 2rem, 2rem 2rem, auto, auto;
    padding: 2.2rem 2.2rem 0;
  }

  .home-platform__visual::after {
    position: absolute;
    inset: auto 0 0;
    height: 28%;
    background: linear-gradient(transparent, hsl(var(--ink) / .08));
    content: "";
    pointer-events: none;
  }

  .home-platform__visual :global(.ui-image) {
    position: relative;
    z-index: 1;
    display: block;
    width: auto;
    max-width: min(88%, 18rem);
    height: calc(100% + .65rem);
    border: 1px solid hsl(var(--line));
    border-bottom: 0;
    border-radius: 1.15rem 1.15rem 0 0;
    background: hsl(var(--canvas));
    box-shadow: 0 1.6rem 3.5rem -1.8rem hsl(var(--ink) / .65);
    object-fit: cover;
    object-position: top center;
  }

  .home-platform__number {
    position: absolute;
    top: 1rem;
    right: 1rem;
    z-index: 2;
    color: hsl(var(--ink-muted));
    font: 700 .67rem/1 var(--santi020k-font-mono, monospace);
    letter-spacing: .08em;
  }

  .home-platform__web-canvas {
    position: relative;
    z-index: 1;
    display: grid;
    width: min(100%, 25rem);
    height: calc(100% - .25rem);
    grid-template-columns: repeat(3, auto);
    grid-template-rows: auto 1fr;
    justify-content: start;
    gap: .55rem;
    overflow: hidden;
    border: 1px solid hsl(var(--line));
    border-bottom: 0;
    border-radius: 1.15rem 1.15rem 0 0;
    background: hsl(var(--canvas));
    box-shadow: 0 1.6rem 3.5rem -1.8rem hsl(var(--ink) / .65);
    padding: 1rem;
  }

  .home-platform__web-canvas > span {
    display: inline-flex;
    align-items: center;
    gap: .35rem;
    border: 1px solid hsl(var(--line) / .8);
    border-radius: 999px;
    background: hsl(var(--surface));
    padding: .4rem .55rem;
    color: hsl(var(--ink-soft));
    font-size: .64rem;
    font-weight: 750;
  }

  .home-platform__web-canvas > span:first-child,
  .home-platform__web-canvas > span:first-child :global(.ui-icon) {
    color: hsl(var(--brand));
  }

  .home-platform__web-canvas > div {
    display: grid;
    grid-column: 1 / -1;
    grid-template-columns: .7fr 1.3fr;
    grid-template-rows: .65fr .35fr;
    gap: .65rem;
    border-top: 1px solid hsl(var(--line) / .72);
    padding-top: 1rem;
  }

  .home-platform__web-canvas i {
    display: block;
    border: 1px solid hsl(var(--line) / .72);
    border-radius: .65rem;
    background: hsl(var(--surface-muted));
  }

  .home-platform__web-canvas i:first-child {
    grid-row: 1 / -1;
  }

  .home-platform__web-canvas i:nth-child(2) {
    background: linear-gradient(135deg, hsl(var(--brand-soft)), hsl(var(--surface)));
  }

  .home-platform__body {
    display: flex;
    min-width: 0;
    flex-direction: column;
    align-items: flex-start;
    padding: clamp(1.25rem, 3vw, 1.75rem);
  }

  .home-platform__meta {
    display: flex;
    width: 100%;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1rem;
  }

  .home-platform__meta > span {
    display: inline-flex;
    min-width: 0;
    align-items: center;
    gap: .45rem;
    color: hsl(var(--ink-muted));
    font-size: .68rem;
    font-weight: 750;
    letter-spacing: .03em;
  }

  .home-platform__meta > span :global(.ui-icon) {
    flex: 0 0 auto;
    color: hsl(var(--brand));
  }

  .home-platform__meta > div {
    display: flex;
    flex: 0 0 auto;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: .35rem;
  }

  .home-platform h3 {
    margin: 1.65rem 0 0;
    color: hsl(var(--ink));
    font-family: var(--app-font-display), sans-serif;
    font-size: clamp(1.55rem, 3vw, 2.1rem);
    font-weight: 780;
    letter-spacing: -.025em;
    line-height: 1.08;
  }

  .home-platform__body > p {
    max-width: 35rem;
    margin: .75rem 0 1.35rem;
    color: hsl(var(--ink-soft));
    font-size: .84rem;
    line-height: 1.65;
  }

  .home-platform__body > code {
    max-width: 100%;
    overflow: hidden;
    border: 1px solid hsl(var(--line) / .78);
    border-radius: .55rem;
    background: hsl(var(--surface-muted) / .72);
    padding: .46rem .58rem;
    color: hsl(var(--ink-soft));
    font-size: .65rem;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .home-platform__body > :global(.ui-button-link) {
    margin-top: auto;
    padding-top: 1.35rem;
  }

  @media (max-width: 960px) {
    .home-proof__inner {
      grid-template-columns: 1fr auto;
    }

    .home-proof__categories {
      grid-column: 1 / -1;
      justify-content: flex-start;
    }

    .home-proof__stats {
      border-right: 0;
    }

    .home-section__head--split {
      grid-template-columns: 1fr;
      align-items: start;
      gap: 1.5rem;
    }

    .home-section__aside {
      max-width: 34rem;
    }

    .home-use-case {
      grid-column: span 6;
    }

    .home-use-case--featured {
      grid-column: 1 / -1;
      grid-template-columns: minmax(0, 1.25fr) minmax(17rem, .75fr);
    }

    .home-ai {
      grid-template-columns: 1fr;
    }

    .home-ai__copy > p {
      max-width: 42rem;
    }

  }

  @media (max-width: 720px) {
    .home-container,
    .home-hero__copy,
    .home-hero__product {
      width: min(100% - 1.5rem, 74rem);
    }

    .home-hero {
      padding-top: 3.75rem;
    }

    .home-hero h1 {
      font-size: clamp(3.15rem, 15vw, 5rem);
    }

    .home-hero__lede {
      font-size: .98rem;
    }

    .home-installer {
      grid-template-columns: 1fr;
      gap: .25rem;
      border-radius: calc(var(--ui-radius) + .35rem);
      padding: .38rem;
    }

    .home-installer__label {
      display: none;
    }

    .home-installer [role="tablist"] {
      width: 100%;
      justify-content: stretch;
    }

    .home-installer [role="tab"] {
      flex: 1;
      padding-inline: .45rem;
      font-size: .78rem;
    }

    .home-installer [role="tabpanel"] {
      border-top: 1px solid hsl(var(--line) / .75);
      border-left: 0;
    }

    .home-install {
      min-height: 2.65rem;
    }

    .home-proof__inner {
      display: grid;
      grid-template-columns: 1fr;
    }

    .home-proof__stats {
      border-left: 0;
      padding-inline: 0;
    }

    .home-proof__categories {
      grid-column: auto;
    }

    .home-specimens,
    .home-principles__grid,
    .home-use-cases__grid {
      display: grid;
      grid-template-columns: 1fr;
    }

    .home-specimen,
    .home-use-case,
    .home-use-case--featured {
      grid-column: auto;
    }

    .home-use-case--featured {
      grid-template-columns: 1fr;
    }

    .home-use-case--featured .home-use-case__stage {
      min-height: 24rem;
      border-right: 0;
      border-bottom: 1px solid hsl(var(--line) / .72);
    }

    .home-specimen {
      min-height: 17rem;
    }

    .home-use-case__body {
      min-height: 0;
    }

    .home-principle {
      min-height: 18rem;
      padding-inline: 1rem;
    }

    .home-principle + .home-principle {
      border-top: 1px solid hsl(var(--line));
      border-left: 0;
    }

    .home-ai__agent-flow {
      grid-template-columns: 1fr;
    }

    .home-platforms__grid {
      grid-template-columns: 1fr;
    }

    .home-platform {
      grid-template-rows: 15rem 1fr;
    }

    .home-platform__visual {
      padding: 1.75rem 1rem 0;
    }

    .home-platform__body {
      min-height: 20rem;
    }

    .home-specimen__swatches span::before {
      height: 4rem;
    }
  }

  @media (max-width: 440px) {
    .home-hero__actions {
      width: 100%;
      flex-direction: column;
    }

    .home-hero__actions :global(.ui-button-link) {
      width: 100%;
    }

    .home-proof__stats {
      justify-content: space-between;
      gap: .8rem;
    }

    .home-specimen__center :global(.ui-button-group) {
      transform: scale(.86);
    }

    .home-platform__meta {
      display: grid;
    }

    .home-platform__meta > div {
      justify-content: flex-start;
    }

    .home-platform__web-canvas {
      grid-template-columns: repeat(3, 1fr);
      padding: .8rem;
    }

    .home-platform__web-canvas > span {
      justify-content: center;
      padding-inline: .35rem;
    }

    .home-platform__web-canvas > span :global(.ui-icon) {
      display: none;
    }
  }
</style>
