---
"@santi020k/lumen-react-native": patch
---

Allow application-defined semantic color values in the public React Native theme contract so
consumer themes can be passed to `LumenProvider` without unsafe casts.
