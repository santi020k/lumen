---
"@santi020k/lumen-mcp": minor
---

Add `@santi020k/lumen-mcp`, a Model Context Protocol server that lets AI agents list components, read real component source and props, search the catalog, and fetch Lumen tokens and agent rules over stdio.
