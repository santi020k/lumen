---
"@santi020k/lumen": patch
---

Align native text-field validation semantics so an error message implies an invalid Compose field,
while SwiftUI preserves the entered accessibility value and announces validation separately.
