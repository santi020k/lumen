---
"@santi020k/lumen": patch
"@santi020k/lumen-astro": patch
"@santi020k/lumen-elements": patch
"@santi020k/lumen-react": patch
---

Add a polymorphic `as` prop to `Stat` so standalone metrics can use an `article` or `section`
root while preserving `div` as the default. Add `default`, `accent`, and `glass` visual variants
across Astro, React, and Web Components.
