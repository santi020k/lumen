---
"@santi020k/lumen-astro": patch
---

Polish Code block layout so headers avoid control overlap, inline code uses the component palette, and framed examples preserve multiline formatting without inheriting global pre borders.
