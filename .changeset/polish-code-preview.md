---
"@santi020k/lumen-astro": patch
---

Polish Code block layout so headers avoid control overlap, inline code uses the component palette, framed examples preserve multiline formatting without inheriting global pre borders, and lightweight token classes can use the active Code theme colors.
