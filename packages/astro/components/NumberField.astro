---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'input'> {
  class?: string
  className?: string
}

const { class: classProp = '', className = '', type = 'number', ...rest } = Astro.props
const passthrough = rest as Record<string, unknown>
---

<input class:list={['ui-input', 'ui-number-field', classProp, className]} type={type} {...passthrough} />
