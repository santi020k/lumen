# @santi020k/lumen-react

React primitives for Lumen UI.

This package currently provides the first shared primitives (`Button`, `Card`, and `Input`) while the Astro package remains the most complete implementation.
